#!/usr/bin/env bash
args="$@"
if [ -z "$args" ]; then
    claude --allowedTools "Task,Edit,Read,WebFetch,WebSearch,Glob,Grep,Write(./**/*),Bash(*)"
else
    claude --allowedTools "Task,Edit,Read,WebFetch,WebSearch,Glob,Grep,Write(./**/*),Bash(*)" -p "$args"
fi