#!/usr/bin/env bash
remote=$(git remote get-url origin 2>/dev/null)
if [ -z "$remote" ]; then
    echo "No origin remote found"
    exit 1
fi
# Convert SSH to HTTPS
url=$(echo "$remote" | sed -e 's|^git@github\.com:|https://github.com/|' -e 's|\.git$||')
echo "Opening $url"
open "$url"
